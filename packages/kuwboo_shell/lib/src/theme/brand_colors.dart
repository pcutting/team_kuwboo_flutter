import 'package:flutter/material.dart';

/// Third-party brand identity colors. These are NOT themed — they must stay
/// constant regardless of palette or dark mode to maintain brand recognition.
class BrandColors {
  BrandColors._();

  static const whatsApp = Color(0xFF25D366);
  static const instagram = Color(0xFFE1306C);
  static const iosMessages = Color(0xFF007AFF);
}

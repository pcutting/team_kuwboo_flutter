name: kuwboo_shell
description: Kuwboo shared shell infrastructure — theme, navigation, state, atoms
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  go_router: ^14.0.0
  google_fonts: ^8.0.0
  phosphor_flutter: ^2.1.0
  lucide_icons_flutter: ^3.1.10
  heroicons_flutter: ^0.4.0
  iconsax_flutter: ^1.0.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

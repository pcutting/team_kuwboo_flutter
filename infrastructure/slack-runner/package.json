{
  "name": "kuwboo-slack-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone PM2 process that listens for agent-run dispatches from the Slack bridge Vercel function, clones the repo, runs the Claude Agent SDK, commits, pushes, and opens a PR.",
  "scripts": {
    "dev": "tsx watch src/runner.ts",
    "start": "tsx src/runner.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "fastify": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}

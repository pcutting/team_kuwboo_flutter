export default function Page() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: 40 }}>
      <h1>Kuwboo Slack Bridge</h1>
      <p>Webhook deployment. Nothing to see here — events flow through <code>/api/slack/events</code>.</p>
    </main>
  );
}

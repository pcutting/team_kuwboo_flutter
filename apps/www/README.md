# apps/www

Plain-HTML static marketing site and legal pages for Kuwboo. No build step — files are served as-is. Deploys to `kuwboo.com` and `www.kuwboo.com` from a dedicated `team_kuwboo_www` Vercel project, separate from the Flutter prototype that serves under `apps/web`.
